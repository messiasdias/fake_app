import $ from "jquery"
import "materialize"

  $(document).ready(function(){
    $('.fixed-action-btn').floatingActionButton();  
    $('select').formSelect();
  });

//alert( Math.ceil(Math.random()*2) )
        